<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.dexon.wallet.walletapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="org.dexon.wallet.walletapp.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <data
                    android:host="send-transaction"
                    android:scheme="dekusan" />
                <data
                    android:host="sign-transaction"
                    android:scheme="dekusan" />
                <data
                    android:host="sign-message"
                    android:scheme="dekusan" />
                <data
                    android:host="sign-personal-message"
                    android:scheme="dekusan" />
                <data
                    android:host="sign-typed-message"
                    android:scheme="dekusan" />

                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
        </activity>
    </application>

</manifest>